{
  "name": "entrance-desktop-workspace",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix Entrance && npm install --prefix Desktop",
    "start": "npm run start --prefix Desktop",
    "dev": "npm run dev --prefix Desktop",
    "pack": "npm run pack --prefix Desktop",
    "dist": "npm run dist --prefix Desktop",
    "dist:linux": "npm run dist:linux --prefix Desktop",
    "dist:win": "npm run dist:win --prefix Desktop"
  }
}
